# Docker build profile for CPU-only deployments.
# This avoids pulling very large CUDA wheel dependencies when installing ultralytics.

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

-r requirements.txt